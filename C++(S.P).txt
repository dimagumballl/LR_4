// Example program
#include <iostream>
#include <string>
using namespace std;

int main()
{
  int a,b,c;
  cout << "What are (a) and (b)?" << "\n";
  cout << "Enter a : " << "\n" ;
  cin >> a;
  cout << "Enter b : "<<"\n";
  cin >> b;
  cout << " S = " << a * b <<  "\n" << " P = "<< a * 2+ b * 2<< "\n" ;
  c=a , a=b, b=c;
  cout << "new a and b "<<a<<" "<<b; 
}
